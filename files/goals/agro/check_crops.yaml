# GoalSpec: check_crops
# Equivalent to CheckCropsTask.java
# Activates when crops are growing.

id: check_crops
version: "1.0"
display_name: "Revisar Cultivos"
description: "Inspect the growing crops for status updates."
pyramid_level: ATTENTION_CYCLE
plan_ref: check_crops_plan_v1

# Check if any land is growing
activation_when: "state.hasLandWithCropCare('CHECK')"

contribution_rules:
  fixed_value: 0.4 # Regular care task
