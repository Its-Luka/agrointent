# GoalSpec: harvest_crops
# Equivalent to HarvestCropsGoal.java
# Activates when there are lands in HARVEST season.

id: harvest_crops
version: "1.0"
display_name: "Cosechar Cultivos"
description: "Harvest the crops that are ready."
pyramid_level: ATTENTION_CYCLE
plan_ref: harvest_crops_plan_v1

# Check if any land is ready for harvest
activation_when: "state.hasLandWithSeason('HARVEST')"

contribution_rules:
  fixed_value: 0.9
