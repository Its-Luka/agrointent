# GoalSpec: manage_pests
# Equivalent to ManagePestsTask.java
# Activates when crops are growing (as a preventative or reactive measure).

id: manage_pests
version: "1.0"
display_name: "Controlar Plagas"
description: "Apply pesticides to protect growing crops."
pyramid_level: ATTENTION_CYCLE
plan_ref: manage_pests_plan_v1

# Check if any land is growing
activation_when: "state.hasLandWithCropCare('PESTICIDE')"

contribution_rules:
  fixed_value: 0.5
