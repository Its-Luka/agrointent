# GoalSpec: communicate
# Equivalent to CommunicateGoal.java
# Currently disabled in the original simulation.

id: communicate
version: "1.0"
display_name: "Comunicación Social"
description: "General social interaction with neighbors."
pyramid_level: NEED
plan_ref: communicate_plan_v1

activation_when: "false"

contribution_rules:
  fixed_value: 0.5
